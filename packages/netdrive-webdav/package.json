{
  "name": "@netdrive/webdav",
  "version": "0.2.0",
  "repository": "https://github.com/valerian-borisovich/netdrive",
  "license": "MIT",
  "main": "dist/index.js",
  "files": ["dist", "src"],
  "scripts": {
    "dev": "tsc -w -p .",
    "build": "rm -rf dist && tsc -p .",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s --commit-path .",
    "release": "node ../../scripts/release.js --commit-path ."
  },
  "dependencies": {
    "xml2js": "^0.4.23"
  },
  "devDependencies": {
    "@types/koa": "^2.13.4",
    "@types/xml2js": "^0.4.9",
    "ts-node": "latest",
    "typescript": "^4.5.7"
  }
}
